# 小猫月度预算账本 PRD

## 一、产品定位

微信小程序帮助用户制定月度预算规划并记录日常开销，通过预算 vs 实际的对比视图，建立消费意识。阶段二新增 Web Agent，基于同一份账本数据提供消费分析和对话式操作。

---

## 二、用户流程总览

```
首次使用
  ↓
微信登录（code2session → JWT）
  ↓
进入首页 → 当月无预算 → 提示设置预算
  ↓
预算编辑页 → 选择/自定义科目 → 填写各科目金额 → 保存
  ↓
返回首页 → 显示当月预算执行总览
  ↓
记一笔 → 选科目 + 填金额 + 选日期 + 备注 → 保存
  ↓
账单页 → 查看统计 / 流水列表
```

**强制预算前置规则：** 点击「记一笔」时，若用户从未设置过任何月份的预算，强制跳转预算设置页，设置完成后自动返回记账。

---

## 三、全局交互规范

**年月选择：** 首页、预算编辑和账单均使用「选择年月」面板，年份与月份分开操作：先切换年份，再从 12 个月矩阵中选择月份，避免超长月份列表；不使用左右箭头逐月翻页。

**记账日期规则：** 所有月份统计、筛选、汇总均以记账条目的「记账日期」所在月为准，而非记录创建时间。用户可为过去日期补记账，但不允许选择未来日期（日期选择器上限为今天）。

**全局错误处理规范：**

| 场景 | 交互方式 |
|------|------|
| 网络断开 / 请求超时 | 顶部或底部 Toast 提示「网络异常，请稍后重试」，自动消失 |
| 服务端 5xx | Toast 提示「服务器异常，请稍后重试」，自动消失 |
| 表单提交失败（如金额为空、科目未选） | 输入框下方内联红色提示文案，逐条显示，表单不重置 |
| 写操作失败（保存预算、保存记账等） | Toast 提示具体失败原因，表单保持当前状态，用户可重试 |
| 操作成功 | 轻量 Toast（如「保存成功」），1.5 秒后自动消失，不阻断后续操作 |
| 页面数据加载失败 | 页面内错误提示 + 「重试」按钮，不使用全局弹窗 |

---

## 四、功能详细设计

### 4.1 登录

- 微信一键授权登录，通过 `wx.login()` 获取 code，后端 code2session 换取 openid，返回 JWT
- 登录页底部展示用户协议、隐私政策入口，点击在小程序内查看全文
- JWT 存本地，后续请求自动携带 `Authorization: Bearer <token>`
- Token 失效时跳回登录页

---

### 4.2 首页（月度预算总览）

**核心展示：**
- 顶部左上角问候语（根据时间动态显示：早上好 / 下午好 / 晚上好）+ 用户昵称（无昵称时显示「朋友」）
- 年月选择器，默认当月；年份与月份分开选择
- 当月总预算、已花费、剩余额度三项汇总数字
- 剩余额度为负数时，金额标红字显示
- 按科目展示卡片列表：科目名 / 已花费 / 预算额度 / 进度条
- 底部圆形浮动加号按钮

**页面状态：**
| 状态 | 展示 |
|------|------|
| 加载中 | 骨架屏 |
| 无预算但有支出 | 显示已花费，剩余显示负数（标红） |
| 无预算且无支出 | 引导卡片「设置本月预算」 |
| 有预算但无支出 | 汇总数字，已花费为 0 |
| 正常 | 完整汇总 + 科目卡片（超预算，剩余显示负数，标红） |
| 加载失败 | 错误提示 + 重试按钮 |

**交互：**
- 点击科目卡片不跳转，仅展示该科目预算执行信息
- 点击「点击查看明细」→ 跳转账单页的流水 Tab，携带首页当前月份，不附加科目筛选
- 点击浮动加号 → 跳转新增开支页
- 右上角「编辑预算」→ 跳转预算编辑页
- 下拉刷新

---

### 4.3 预算编辑

**入口：** 首页「设置预算」引导 / 首页「编辑预算」按钮

**功能：**
- 顶部年月选择器，年份与月份分开选择
- 当月已有预算时加载已有数据；当月无预算时自动继承上月预算（显示提示横幅「已展示最近一次预算，可直接修改后保存」）；若上月也无预算，则向前查找最近一个有预算的月份并继承，若历史均无预算则从空白开始
- 已选科目列表：每行显示科目名 + 金额输入框 + 删除按钮，支持上下拖拽排序；拖动时当前行随手指移动、经过的行平滑让位，松手后保存新顺序。科目顺序按月独立保存，保存后该月的首页科目卡片、记账科目选择、账单统计科目列表均按此顺序排列
- 添加科目区：
  - **科目选择**：多行自动换行的科目矩阵，展示所有可用科目，已添加的标记 ✓
  - **自定义科目输入框**：输入名称 → 点击「添加」→ 成功后自动加入当前预算列表并出现在科目选择中
- 保存：科目列表为空时禁止保存，提示「请至少添加一个科目」；有科目时所有金额必须 > 0，否则逐条提示

**业务规则：**
- 年月选择器上限为当月，不支持提前设置未来月份预算
- 同一科目在同一月只能出现一次
- 自定义科目命名不能与已有科目重复，重名时提示用户
- 删除科目仅从当前预算移除，不影响科目本身
- 切换年月时若当前有未保存更改，弹窗提示「数据未保存，是否切换月份？」；选「是」丢弃更改并切换，选「否」留在当前月继续编辑

---

### 4.4 开销记账

**入口：** 首页「记一笔」浮动按钮

**新增开支表单：**
| 字段 | 规则 |
|------|------|
| 科目 | 从当前用户可用科目选择；同时支持直接输入新科目名称创建 |
| 金额 | 使用手机原生数字键盘输入；正数，保留两位小数，支持清空后重新输入；前后端双重校验 |
| 日期 | 默认今天，可通过日期选择器修改 |
| 备注 | 可选，最多 100 字 |

**编辑记账：** 从账单列表点击进入，预填原始数据，支持修改所有字段；编辑时不触发预算检查

**删除记账：** 编辑页底部删除按钮，二次确认弹窗，确认后软删除并返回

**强制预算前置规则（入口拦截）：** 点击「记一笔」时，若用户从未设置过任何月份的预算，强制跳转预算设置页，不给跳过选项；设置完成后自动返回记账页。

**记账内预算引导：** 填写记账表单时，若记账日期所在月无预算配置，弹窗提示，用户可选「去设置」或「直接保存」。选「直接保存」后该月内不再重复弹窗，后续在该月记账均可直接保存。

---

### 4.5 账单统计（账单 Tab）

账单页分两个 Tab：**统计** 和 **流水**，顶部使用年份与月份分开操作的年月选择面板，默认进入统计 Tab。

**统计 Tab：**
- **空状态：** 当月无任何支出时，整页仅展示「本月暂无支出记录」，隐藏预算使用率模块、饼图、汇总和科目列表
- **预算使用率模块：** 顶部展示横向进度条，左侧「已支出 / 总预算」金额，右侧百分比；若当月无预算则隐藏此模块；超预算时进度条标红，百分比超 100% 正常显示
- 环形饼图（按科目金额占比分配颜色）
- 三项汇总：本月总支出、笔数、日均支出
- 按科目汇总列表：科目名 / 总金额 / 笔数 / 占比 / 色块；不分页，全部展示

**流水 Tab：**
- **空状态：** 当月无任何支出时，显示「本月暂无支出记录」
- 顶部科目筛选选项，默认展示全部；用户可手动选择某个科目进行过滤；切换月份后科目筛选重置为全部
- 按日期分组，每组显示日期标签（今天/昨天/M月D日）+ 当日小计
- 每条记录：科目名 / 金额 / 备注 / 日期
- 下拉刷新，触底加载更多（分页，每页 20 条）
- 点击记录跳转编辑页

**从首页查看明细跳转：** 携带首页当前选中月份，直接进入流水 Tab，默认展示全部科目

---

### 4.6 科目管理

**当前设计：平级科目，不分层级。**

**系统预置科目（不可删除）：**
饮食、交通、租房、通信、其他

**用户自定义科目：**
- 在预算编辑页或记账页直接输入科目名称创建
- 创建时若科目名已存在（系统预置或用户已创建），不重复创建，直接复用
- 创建成功后立即可用于预算和记账

**为什么不做独立科目管理页？**
用户创建科目的场景发生在「做预算」和「记账」两处，内联创建比跳转到另一个设置页更自然。科目删除暂不提供入口。

---

### 4.7 我的（Profile）

**头部信息区：**
- 显示用户头像、昵称、注册时间
- 头像使用应用内置的卡通小猫头像，默认使用「橘猫」；编辑面板以 3×3 九宫格提供「橘猫、黑猫、三花猫、灰猫、白猫、布偶、奶牛、暹罗、虎斑」九种头像供用户切换，不支持微信头像获取或本地图片上传
- 点击头像或昵称区域弹出编辑面板；昵称输入框使用微信昵称填写能力，用户聚焦输入框后可从键盘上方快捷填入微信昵称，也可直接输入自定义昵称
- 编辑完成后调用 `PUT /api/user/profile` 保存，成功后刷新页面头部

**功能入口：**
- 预算设置：点击跳转预算编辑页（`pages/plan/edit/edit`）
- 联系客服：点击弹出 Modal，展示公众号二维码图片，提示「扫码关注公众号，可在公众号内反馈问题」

**移除项：**
- 不设科目管理独立入口（科目创建内联在预算编辑和记账页，详见 4.6）
- 不设个人信息独立页（头像昵称在本页内联编辑）
- 不提供退出登录操作

---

### 4.8 协议页

- 用户协议、隐私政策在小程序内展示
- 上架前需替换为正式文本，并在微信小程序后台「用户隐私保护指引」填写对应 URL

### 4.9 Web Agent（阶段二）

**定位：** Web Agent 是账本的对话式助手，可查询预算、账单和趋势；新增、编辑或删除账本数据时必须先展示操作卡片并由用户确认。Web 与小程序使用同一个账本用户。

**电脑端登录：**

1. 用户在浏览器打开 Agent Web；
2. Web 创建 2 分钟有效、单次使用的登录请求，并展示固定小程序码与本次六位数字码；
3. 用户扫描固定小程序码进入小程序「确认网页登录」页，输入六位数字码后核对浏览器及设备摘要，再确认或拒绝；
4. 确认后浏览器用仅本机持有的凭据交换短期 Web Cookie 并自动进入对话页。

登录请求过期、拒绝或已使用后均不可再次交换。六位数字码仅展示给创建请求的浏览器，服务端只保存其摘要；固定小程序码由发布前工具生成，并完成真机验证。数据库只保存浏览器凭据、登录码和短链票据的摘要。

**手机端进入 Web：**

- 个人主体不依赖 `web-view`、URL Link 或 URL Scheme，也不要求网页唤起小程序。
- 「我的」页提供「网页版 Agent」入口。已登录用户生成 60 秒有效、单次使用的短链并复制，再自行切换到任意浏览器粘贴打开。
- 小程序无法直接启动系统浏览器，入口文案不得写为「打开浏览器」。
- 一次性票据位于 URL fragment，不包含小程序 JWT；Web 交换完成后清除 fragment。

**Agent 能力与交互：**

- 可查询月度预算执行、账单明细、科目汇总和近期趋势。
- 可准备新增/编辑/删除开支、保存预算和创建科目；写工具只创建 `PENDING_CONFIRMATION` 动作，确认前不得写账本。
- 确认卡片展示可读业务摘要和已校验参数；重复确认只执行一次，过期、取消或目标账本已变化时不执行。
- 可新建会话、切换会话、刷新恢复历史；模型失败或网络中断时保留用户消息并允许重试。
- 输入栏提供「深度思考」开关，默认关闭并仅保存在当前浏览器。开启对应 `reasoning.effort=low`，关闭对应 `none`。
- 推理内容和内部工具消息不展示、不持久化、不进入 SSE、日志或审计表；仅在一次 DeepSeek 工具循环的内存中暂存。
- 不提供 RAG、向量库、消息队列、任意 SQL、文件/代码工具、DeepSeek 内置网页搜索或 Web 账本表单。

---

## 五、页面结构

```
pages/
├── login/          微信登录
├── home/           首页（月度预算执行总览）
├── expense/
│   ├── add/        新增 / 编辑开销
│   └── stats/      账单统计（统计 Tab + 流水 Tab）
├── plan/
│   └── edit/       预算编辑（含自定义科目创建）
├── profile/        我的
├── auth/
│   └── web-login/  网页版 Agent 登录确认 / 扫码登录
└── agreement/      用户协议 / 隐私政策
```

---

## 六、科目设计说明

**为什么不分一二级科目？**

引入层级会带来三个复杂问题：预算应设在哪一级、记账归并逻辑、删除联动。当前方案：科目平级 + 备注字段。

- 预算设「饮食」2000
- 记账选「饮食」，备注写「火锅」或「奶茶」
- Agent 后续可通过备注 NLP 做细粒度分析，数据库和小程序逻辑保持简单

如后续强需求二级科目，可给科目表增加 `parent_id`，但当前阶段不做。

---

## 七、验收标准

1. 微信登录正常，JWT 过期自动跳回登录页
2. 首页问候语根据时间动态显示，用户昵称正确展示；无昵称时显示「朋友」
3. 首页、预算编辑和账单的年月选择均使用年份、月份分开操作的选择面板
4. 首次进入从未设置过预算 → 点击「记一笔」强制跳转预算设置页；记账日期所在月无预算 → 表单内弹窗引导，可选「去设置」或「直接保存」，选「直接保存」后该月不再重复弹窗
5. 无预算但有支出时，剩余额度显示为红色负数
6. 预算编辑：科目多行矩阵、自定义科目创建（重名不重复添加）、金额填写、保存覆盖均正常
7. 新月份自动继承最近一个有预算月份的数据，提示横幅正确显示；历史均无预算时从空白开始
8. 预算编辑页切换月份时若有未保存更改，弹窗拦截，选「是」切换，选「否」留原月
9. 预算科目列表支持上下拖拽排序，保存后该月的首页科目卡片、记账科目选择、账单统计科目列表均按此顺序展示；科目顺序按月独立存储
10. 新增开支的科目支持从已有科目选择，也支持直接输入新科目名创建；金额使用手机原生键盘且支持清空
11. 记账增删改正常，金额只接受正数
12. 账单统计页：当月无支出时显示空状态「本月暂无支出记录」；有支出时展示预算使用率模块（进度条 + 已支出/总预算 + 百分比，超预算标红，无预算时隐藏该模块）、饼图、三项汇总、科目列表，数据与实际一致
13. 账单流水按日期分组、支持分页加载；流水 Tab 顶部科目筛选默认展示全部，切换月份后重置为全部；从首页「点击查看明细」跳转时携带首页当前月份并展示全部科目
14. 「我的」页面：点击头像/昵称区域弹出编辑面板；默认显示橘猫头像，以 3×3 九宫格展示九种内置小猫头像供切换；昵称输入框支持用户快捷填入微信昵称或直接自定义，保存后页面头部刷新；不支持头像上传或微信头像获取
15. 「我的」页面：点击「预算设置」跳转预算编辑页
16. 「我的」页面：点击「联系客服」弹出 Modal，展示公众号二维码并显示引导文案
17. 「我的」页面：不含科目管理、个人信息、退出登录入口
18. 电脑端固定小程序码与六位登录码、一次性短链均遵守有效期与单次使用约束；错误浏览器凭据、重复交换、过期和拒绝均被拒绝
19. Web Cookie 的非安全请求缺少或提交错误 CSRF Token 时返回 403；小程序 Bearer 接口保持兼容，且两种认证来源不能跨端调用
20. 普通与深度思考均可完成流式问答和工具循环；浏览器、SSE、数据库和日志均不出现模型推理内容
21. 四类只读能力只通过现有业务 Service 读取当前用户数据；五类写能力确认前不写入，重复、过期、取消和 `STALE` 均不覆盖账本
22. 会话可新建、切换和刷新恢复；DeepSeek 429、总超时、断流和超长输出返回可重试错误且保留用户消息
23. 固定小程序码与六位登录码流程由发布前人工完成真机验证；固定码须能打开「确认网页登录」页

---

## 八、后续 Agent 接入预留

当前数据模型已为 Agent 分析预留基础：

- 可获取任意月份的预算执行情况（预算 vs 实际对比）
- 可获取月度按科目的开销汇总和多月趋势
- 开销备注字段支持细粒度描述，供后续 NLP 分析消费习惯

阶段二 Web Agent 使用 DeepSeek API，可查询预算执行、账单明细、科目汇总与近期趋势。新增、编辑、删除开支、保存预算和创建科目必须先展示确认卡片，用户确认后才执行；重复、过期或目标数据已变化的动作不得执行。聊天页提供默认关闭的“深度思考”开关，推理内容不展示、不落库、不写日志。电脑端通过固定小程序码与六位登录码确认登录；手机端可从小程序复制 60 秒一次性登录链接。

接口定义详见服务端 ARCHITECTURE.md。
