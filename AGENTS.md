# 小猫月度预算账本

## 项目边界

- 本仓库包含三个部分：`planning-assistant-server/`（Spring Boot 后端）、`planning-assistant-miniapp/`（原生微信小程序）和 `planning-assistant-web/`（阶段二 Agent Web 客户端，目前只有设计文档）。
- 不要在未明确要求时实现阶段二 Agent 或 Web 客户端；先与用户确认技术选型和交付范围。
- 修改应限定在用户指定的模块。跨端变更要同步检查服务端接口、小程序调用和 `api-contract.yaml`。

## 规范与文档

- 产品行为以 `PRD.md` 为准；接口契约以 `api-contract.yaml` 为准；模块边界、数据模型和运行方式以相应的 `ARCHITECTURE.md` 为准。发现冲突时，先报告并在同一次改动中统一文档，不能静默选择一方。
- `AGENTS.md` 仅存放持续有效的开发约束；不要把具体业务流程、DDL 或接口实现细节复制到这里。
- 更新公开 API、数据模型或用户流程时，同步更新对应的架构文档、PRD 或接口契约。

## 跨模块约束

- 金额：后端一律使用 `BigDecimal`，禁止 `float` / `double`；数据库使用精确小数；对外以保留两位小数的字符串传输。
- 时间：后端使用 `LocalDate` / `LocalDateTime`，禁止 `java.util.Date`；年月格式为 `yyyy-MM`，日期格式为 `yyyy-MM-dd`。
- HTTP 响应使用 `Result<T>`；分页数据使用 `PageData<T>`，字段为 `total`、`page`、`pageSize`、`list`。
- 不提交密钥、JWT、数据库密码或真实用户数据。新增配置应通过环境变量注入，并记录所需变量。
- 新增逻辑前先检索现有实现；已在两个及以上位置使用、且无业务状态耦合的逻辑，应提取到对应端的共享工具、组件或服务，避免复制粘贴。不要为单次使用或强业务耦合流程过度抽象。

## 交付与验证

- 开始修改前先阅读目标模块的 `AGENTS.md` 和相关设计文档。
- 优先做最小、可验证的改动；不重写无关代码，不删除用户已有改动。
- 每次改动后运行与影响范围相符的检查，并在交付时明确说明已运行和未运行的验证。
- 当前未发现自动化测试；新增或修复关键业务逻辑时，应补充相应测试，或说明无法自动验证的原因。
