/**
 * API 运行配置示例。
 *
 * 复制为 config.js 后填写实际地址。config.js 已被 Git 忽略，不能缺失，
 * 因为 utils/request.js 会在构建时读取该文件。
 */
const API_BASE_URL = 'https://api.example.com/api';

module.exports = { API_BASE_URL };
